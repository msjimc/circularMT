Ubuntu
Preparation
If your system is 64 bit, enable 32 bit architecture (if you haven't already):
sudo dpkg --add-architecture i386 

Add the repository
Download and add the repository key:

sudo mkdir -pm755 /etc/apt/keyrings
sudo wget -O /etc/apt/keyrings/winehq-archive.key https://dl.winehq.org/wine-builds/winehq.key


Ubuntu 24.04	(Noble Numbat)
sudo wget -NP /etc/apt/sources.list.d/ https://dl.winehq.org/wine-builds/ubuntu/dists/noble/winehq-noble.sources

Ubuntu 23.10	(Mantic Minotaur)
sudo wget -NP /etc/apt/sources.list.d/ https://dl.winehq.org/wine-builds/ubuntu/dists/mantic/winehq-mantic.sources

Ubuntu 22.04	(Jammy Jellyfish)	Linux Mint 21.x
sudo wget -NP /etc/apt/sources.list.d/ https://dl.winehq.org/wine-builds/ubuntu/dists/jammy/winehq-jammy.sources

Ubuntu 20.04	(Focal Fossa)	Linux Mint 20.x
sudo wget -NP /etc/apt/sources.list.d/ https://dl.winehq.org/wine-builds/ubuntu/dists/focal/winehq-focal.sources

Install Wine
Install one of the following packages:
Stable branch	
sudo apt install --install-recommends wine-stable

uninstall: sudo apt-get --purge remove wine wine64 libwine fonts-wine

wine uninstaller:
press 'install' and select the msi
https://dl.winehq.org/wine/wine-mono/
https://dl.winehq.org/wine/wine-mono/9.1.0/wine-mono-9.1.0-x86.msi

wine ~/Downloads/circularMT.exe

Centos
https://medium.com/@sumitdhattarwal4444/running-windows-applications-and-games-on-centos-rhel-with-wine-9784a53cd8f7
Step 1: Installing Dependencies:

Before installing Wine, we need to ensure that all the necessary dependencies are in place. Open a terminal and execute the following command:

sudo yum -y groupinstall 'Development Tools'

Step 2: Downloading Wine Source Code and Development Libraries:

To begin, we need to download the Wine source code and additional development libraries. Run the following command:
sudo yum install gcc libX11-devel freetype-devel zlib-devel libxcb-devel libxslt-devel libgcrypt-devel libxml2-devel gnutls-devel libpng-devel libjpeg-turbo-devel libtiff-devel dbus-devel fontconfig-devel

Step 3: Extracting and Preparing Wine Source Code:

Navigate to the temporary directory and extract the Wine source code using the following commands:
cd /tmp
wget http://dl.winehq.org/wine/source/7.0/wine-7.0.tar.xz
tar -xvf wine-7.0.tar.xz -C /tmp/
cd wine-7.0/

Step 4: Configuring, Building, and Installing Wine:

Now it’s time to configure, build, and install Wine. Run the following commands:

./configure --enable-win64
make
sudo make install

let winecfg install mono at dialogue prompt


Final: Using Wine to Run Windows Apps & Games:

Once Wine is installed, you can use it to run Windows applications and games on your CentOS/RHEL system. To configure Wine, use the following command:

winecfg

wine64 circularMT_64.exe

openSUSE

open YaST in programs and enter wine